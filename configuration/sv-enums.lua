Providers = {
    monitor = 'txAdmin',
}

Punishments = {
    kick = true,
    warn = true,
    ban = true
}

APIUrl = 'http://localhost:8080/api/v1/'

Routes = {
    log = 'create',
    get = 'get',
    -- revoke = 'revoke',
}
